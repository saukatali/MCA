// find area of reactengle
#include <stdio.h>

int main() {

    float l, b, a;

    printf("Enter l and b \n: ");
    scanf("%f %f", &l, &b);

    a =l*b;

    printf("Area of triangle is: %f\n", a);

    return 0;
}
